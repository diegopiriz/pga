<%= form_for([@dog, @dog.dog_pictures.build], :html => {:multipart => true}) do |f| %>
  <div class="field">
    <%= f.label :data %>
    <%= f.file_field :data %>
  </div>
  <div class="actions">
    <%= f.submit "Upload" %>
  </div>
<% end %>