<!-- NOTICE -->
<% if notice %>
	<br />
	<p id="notice"><%= notice %></p>
<% end %>

<div>
  <!-- ADMISSION -->
  <p class="pull-right">
    <strong><%=t 'admission'%>:</strong>
    <%=l @dog.admission, format: :long %>
  </p>
  <!-- NAME -->
  <h1><%= @dog.name %></h1>
</div>
<!-- CAROUSEL -->
<div class="jumbotron">
  <div class="row">
  <% 4.times do %>
    <div class="col-md-3">
      <%= image_tag("generic_dog.jpg", size: "171x180") %>
    </div>
  <% end %>
  </div>
</div>

<table>
  <!-- STATUS -->
  <tr>
    <th class="text-right"><%=t 'status' %>:</th>
    <td class="capitalize"><%=Status.find_by(id: @dog.status_id).description %></td>
  </tr>
  <!-- AGE -->
  <tr>
    <th class="text-right"><%=t 'age' %>:</th>
    <td>
      <%
        age = age_in_months @dog.birtdate 
        years = years age
        months = months age
      %>
      <% if years != 0%>
        <%=t 'age_in_years_and_months', years: years, months: months %>
      <% else %>
        <%=t 'age_in_months', months: months %>
      <% end %>
    </td>
  </tr>
  <!-- SIZE -->
  <tr>
    <th class="text-right"><%=t 'size' %>:</th>
    <td class="capitalize"><%=Size.find_by(id: @dog.size_id).description %></td>
  </tr>
  <!-- COLORS -->
  <tr>
    <th class="text-right"><%=t 'primary_color' %>:</th>
    <td class="capitalize"><%= color_name @dog.primary_color%></td>
  </tr>
  <tr>
    <th class="text-right"><%=t 'secondary_color' %>:</th>
    <td class="capitalize"><%= color_name @dog.secondary_color%></td>
  </tr>
  <!-- ORIGIN -->
  <tr>
    <th class="text-right" valign="top"><%=t 'origin' %>:</th>
    <td><%= @dog.origin %></td>
  </tr>
  <!-- PERSONALITY -->
  <tr>
    <th class="text-right" valign="top"><%=t 'personality' %>:</th>
    <td><%= @dog.personality %></td>
  </tr>
</table>

<div class="pull-right">
  <%= link_to I18n.t('edit'), edit_dog_path(@dog) %> |
  <%= link_to I18n.t('back'), dogs_path %>
</div>